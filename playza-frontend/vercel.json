{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!gameLib|assets|thumbnails|logoImage.png|hero.png).*)",
      "destination": "/"
    }
  ]
}
